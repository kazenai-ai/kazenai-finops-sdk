# Disabled for public-candidate safety (LEGAL LF-11 / F10).
# Cross-repo clone + contents:write + scheduled auto-commit is not suitable
# as a default public SDK workflow. Re-enable only with explicit founder approval
# scoped to approved public inputs and least privilege.
name: LLM Guard Badge (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow is disabled pending LF-11 public-candidate hardening."
          exit 0
